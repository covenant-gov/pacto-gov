name: CI

on: [push]

concurrency:
  group: ${{github.workflow}}-${{github.ref}}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6

      - name: install-tools
        uses: ./.github/workflows/install-tools

      - name: Run tests
        shell: bash
        run: pnpm test:unit

  integration-tests:
    name: Run Integration Tests
    runs-on: ubuntu-latest
    # `environment:` MUST match the slug under Settings → Environments where MAINNET_RPC lives.
    environment: integration
    env:
      _GHA_MAINNET_RPC_SECRET: ${{ secrets.MAINNET_RPC }}
      _GHA_MAINNET_RPC_VAR: ${{ vars.MAINNET_RPC }}
      _GHA_SEPOLIA_RPC_SECRET: ${{ secrets.SEPOLIA_RPC }}
      _GHA_SEPOLIA_RPC_VAR: ${{ vars.SEPOLIA_RPC }}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6

      - name: install-tools
        uses: ./.github/workflows/install-tools

      - name: Run E2E tests
        run: pnpm test:integration:ci

  lint:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: wagoid/commitlint-github-action@v5
      - name: install-tools
        uses: ./.github/workflows/install-tools

      - name: Run Forge formatter and Solhint
        run: pnpm lint:check

      - name: Install and run Bulloak
        uses: smol-ninja/bulloak-toolchain@v2
        with:
          test-dir: 'test/unit'
